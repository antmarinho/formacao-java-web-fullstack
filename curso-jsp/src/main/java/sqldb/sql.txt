CREATE DATABASE "curso-jsp"
    WITH
    OWNER = acgm
    ENCODING = 'UTF8'
    LC_COLLATE = 'Portuguese_Brazil.1252'
    LC_CTYPE = 'Portuguese_Brazil.1252'
    LOCALE_PROVIDER = 'libc'
    TABLESPACE = pg_default
    CONNECTION LIMIT = -1
    IS_TEMPLATE = False;
    
CREATE TABLE IF NOT EXISTS public."user"
(
    login character varying(200) COLLATE pg_catalog."default" NOT NULL,
    pass character varying(200) COLLATE pg_catalog."default" NOT NULL,
    id integer NOT NULL DEFAULT nextval('user_id_seq'::regclass),
    nome character varying(300) COLLATE pg_catalog."default" NOT NULL,
    email character varying(300) COLLATE pg_catalog."default" NOT NULL,
    CONSTRAINT user_pkey PRIMARY KEY (id),
    CONSTRAINT login_pk UNIQUE (login)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public."user"
    OWNER to acgm;
    
INSERT INTO public."user"(login, pass,nome,email)
	VALUES ('admin', 'admin',,'acgm','acgm@acgm.com');
	
ALTER TABLE "user" ADD COLUMN useradmin NOT NULL BOOLEAN DEFAULT false;

ALTER TABLE "user" ADD COLUMN id_user BIGINT NOT NULL DEFAULT 1;

ALTER TABLE "user" ADD COLUMN perfil character varying(200);

ALTER TABLE "user" ADD COLUMN sexo character varying(2);

ALTER TABLE "user" ADD COLUMN fotouser text;

ALTER TABLE "user" ADD COLUMN extensaofoto character varying(15);

ALTER TABLE "user" ADD COLUMN cep character varying(15);

ALTER TABLE "user" ADD COLUMN logradouro character varying(200);

ALTER TABLE "user" ADD COLUMN bairro character varying(200);

ALTER TABLE "user" ADD COLUMN localidade character varying(200);

ALTER TABLE "user" ADD COLUMN uf character varying(3);

ALTER TABLE "user" ADD COLUMN complemento character varying(200);

ALTER TABLE "user" ADD COLUMN numero character varying(15);

ALTER TABLE "user" ADD CONSTRAINT fk_user FOREIGN KEY (id_user) REFERENCES "user" (id);

CREATE SEQUENCE telefone_seq
INCREMENT 1
MINVALUE 1
MAXVALUE 9223372036854775807
START 1
CACHE 1;

CREATE TABLE telefone 
(

	id integer NOT NULL DEFAULT nextval('telefone_seq'::regclass),
	numero character varying(50) NOT NULL,
	user_id bigint NOT NULL,
	user_cad_id bigint NOT NULL,
	
	CONSTRAINT telefone_pkey PRIMARY KEY (id),
	CONSTRAINT user_id_fk FOREIGN KEY (user_id) REFERENCES "user" (id),
	CONSTRAINT user_cad_fk FOREIGN KEY (user_id) REFERENCES "user" (id),

)

